const defaultBoard = [
  [-1, 1, 0],
  [1,  1, 0],
  [0,  0, 0],
]


export function  createMockRandomGameBoard(size?: number, bomds?: number): number[][] {
  return defaultBoard
}
export function createMockBoardData(mineBoard: number[][]) {
  return defaultBoard
}
